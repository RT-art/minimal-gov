# 初回は local で apply → バケット作成後に値を埋めて使う
bucket = "<after-apply-fill-me>"            # 例: remote-backend-s3-tfstate-ap-northeast-1-123456789012
key    = "tfstate/remote-backend/ap-northeast-1/dev/terraform.tfstate"
region = "ap-northeast-1"
encrypt = true
# profile = "default"  # 必要なら
